# 原始照片目录说明

为避免将现有 96 张原始 JPEG 复制一份而额外增加约 87.6 MiB 仓库体积，原始照片暂保留在项目既有的 `摄影/`、`剧照/` 与 `证件照/` 目录中，作为本目录的逻辑来源。

页面只引用同级的 `thumbnail/`、`medium/`、`large/` 响应式衍生图。迁移到对象存储或 CDN 时，可将这三个原始目录整体移动/同步至本目录，而无需修改 `works-data.js` 中的作品数据。
